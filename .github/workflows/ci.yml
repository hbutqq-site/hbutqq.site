name: CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]
  workflow_dispatch:

concurrency:
  group: ci-${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  quality:
    name: Quality (lint / format / typecheck)
    runs-on: ubuntu-latest
    timeout-minutes: 15
    if: github.event_name != 'push'
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup pnpm environment
        uses: ./.github/actions/setup-pnpm

      - name: Lint
        run: pnpm lint

      - name: Format check
        run: pnpm format:check

      - name: Typecheck
        run: pnpm typecheck

      - name: Check Trellis tasks are archived
        shell: bash
        run: |
          unarchived=$(find .trellis/tasks -mindepth 1 -maxdepth 1 -type d ! -name archive 2>/dev/null || true)
          if [ -n "$unarchived" ]; then
            echo "::error::.trellis/tasks/ 下除 archive/ 外不得存在其他一级目录；Trellis 任务必须归档后才能提交至仓库"
            echo "未归档任务目录："
            printf '%s\n' "$unarchived"
            exit 1
          fi
          echo "OK: .trellis/tasks/ 仅包含 archive/"

  unit:
    name: Unit tests (Vitest)
    runs-on: ubuntu-latest
    timeout-minutes: 15
    if: github.event_name != 'push'
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup pnpm environment
        uses: ./.github/actions/setup-pnpm

      - name: Run unit tests
        run: pnpm test

  workers:
    name: Worker integration tests
    runs-on: ubuntu-latest
    timeout-minutes: 15
    if: github.event_name != 'push'
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup pnpm environment
        uses: ./.github/actions/setup-pnpm

      - name: Run worker tests
        run: pnpm test:workers

  build:
    name: Production build
    runs-on: ubuntu-latest
    timeout-minutes: 15
    if: github.event_name != 'push'
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup pnpm environment
        uses: ./.github/actions/setup-pnpm

      - name: Build
        run: pnpm build

  e2e-browser-cache:
    name: E2E browser cache seed (${{ matrix.browser }})
    if: github.event_name == 'push'
    runs-on: ubuntu-latest
    timeout-minutes: 15
    strategy:
      fail-fast: false
      matrix:
        browser: [chromium, webkit, firefox]
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup pnpm environment
        uses: ./.github/actions/setup-pnpm

      - name: Resolve Playwright version
        id: playwright-version
        shell: bash
        run: |
          version=$(node -p "require('./node_modules/@playwright/test/package.json').version")
          echo "version=$version" >> "$GITHUB_OUTPUT"

      - name: Restore main browser cache
        id: cache-main
        uses: actions/cache/restore@v4
        with:
          path: ~/.cache/ms-playwright
          key: ms-playwright-main-${{ runner.os }}-${{ steps.playwright-version.outputs.version }}-${{ matrix.browser }}

      - name: Install Playwright browser
        shell: bash
        run: pnpm exec playwright install ${{ matrix.browser }}

      - name: Save main browser cache
        if: steps.cache-main.outputs.cache-hit != 'true'
        uses: actions/cache/save@v4
        with:
          path: ~/.cache/ms-playwright
          key: ms-playwright-main-${{ runner.os }}-${{ steps.playwright-version.outputs.version }}-${{ matrix.browser }}

  e2e-main:
    name: E2E main (shard ${{ matrix.shard }}/3)
    runs-on: ubuntu-latest
    timeout-minutes: 30
    if: github.event_name != 'push'
    strategy:
      fail-fast: false
      matrix:
        shard: [1, 2, 3]
        browser: [chromium]
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup pnpm environment
        uses: ./.github/actions/setup-pnpm

      - name: Resolve Playwright version
        id: playwright-version
        shell: bash
        run: |
          version=$(node -p "require('./node_modules/@playwright/test/package.json').version")
          echo "version=$version" >> "$GITHUB_OUTPUT"

      - name: Record cache restore start
        id: cache-restore-start
        shell: bash
        run: echo "ts=$(date +%s)" >> "$GITHUB_OUTPUT"

      - name: Restore main browser cache
        id: cache-main
        uses: actions/cache/restore@v4
        with:
          path: ~/.cache/ms-playwright
          key: ms-playwright-main-${{ runner.os }}-${{ steps.playwright-version.outputs.version }}-${{ matrix.browser }}

      - name: Restore PR browser cache
        id: cache-pr
        if: steps.cache-main.outputs.cache-hit != 'true' && github.event_name == 'pull_request'
        uses: actions/cache/restore@v4
        with:
          path: ~/.cache/ms-playwright
          key: ms-playwright-pr-${{ runner.os }}-${{ steps.playwright-version.outputs.version }}-${{ matrix.browser }}

      - name: Record cache restore end
        id: cache-restore-end
        shell: bash
        run: echo "ts=$(date +%s)" >> "$GITHUB_OUTPUT"

      - name: Record browser prep start
        id: browser-prep-start
        shell: bash
        run: echo "ts=$(date +%s)" >> "$GITHUB_OUTPUT"

      - name: Install system dependencies
        shell: bash
        run: pnpm exec playwright install-deps ${{ matrix.browser }}

      - name: Install Playwright browser
        if: steps.cache-main.outputs.cache-hit != 'true' && steps.cache-pr.outputs.cache-hit != 'true'
        shell: bash
        run: pnpm exec playwright install ${{ matrix.browser }}

      - name: Save PR browser cache
        if: github.event_name == 'pull_request' && steps.cache-main.outputs.cache-hit != 'true' && steps.cache-pr.outputs.cache-hit != 'true' && matrix.shard == 1
        uses: actions/cache/save@v4
        with:
          path: ~/.cache/ms-playwright
          key: ms-playwright-pr-${{ runner.os }}-${{ steps.playwright-version.outputs.version }}-${{ matrix.browser }}

      - name: Record browser prep end
        id: browser-prep-end
        shell: bash
        run: echo "ts=$(date +%s)" >> "$GITHUB_OUTPUT"

      - name: Generate E2E test env file
        shell: bash
        run: |
          cat > tests/e2e/.dev.vars <<'EOF'
          ADMIN_PASSWORD=test-admin-password
          SESSION_SECRET=test-session-secret
          LIKE_PEPPER=test-like-pepper
          DEV_LIKE_PEPPER=test-like-pepper
          TURNSTILE_SECRET_KEY=test-turnstile-secret
          SKIP_TURNSTILE=true
          SECURE_COOKIE=false
          LOGIN_MAX_ATTEMPTS=100
          LOGIN_WINDOW_MINUTES=5
          EOF

      - name: Run E2E tests
        shell: bash
        run: |
          set -o pipefail
          pnpm exec playwright test --shard=${{ matrix.shard }}/3 --project=chromium-desktop --project=chromium-mobile 2>&1 | tee playwright-main-${{ matrix.shard }}.log

      - name: Summarize shard run
        if: always()
        shell: bash
        run: |
          {
            echo "## E2E main shard ${{ matrix.shard }}/3"
            echo
            echo "| metric | value |"
            echo "|---|---|"
            echo "| main-cache-hit | ${{ steps.cache-main.outputs.cache-hit }} |"
            echo "| pr-cache-hit | ${{ steps.cache-pr.outputs.cache-hit || 'n/a' }} |"
            echo "| cache-restore-seconds | $(( ${{ steps.cache-restore-end.outputs.ts }} - ${{ steps.cache-restore-start.outputs.ts }} )) |"
            echo "| browser-prep-seconds | $(( ${{ steps.browser-prep-end.outputs.ts }} - ${{ steps.browser-prep-start.outputs.ts }} )) |"
            echo "| browser-prep-total-seconds | $(( ${{ steps.browser-prep-end.outputs.ts }} - ${{ steps.cache-restore-start.outputs.ts }} )) |"
            echo "| browsers-size | $(du -sh ~/.cache/ms-playwright 2>/dev/null | cut -f1 || echo n/a) |"
            node -e "const fs=require('fs');const report='./playwright-report/results.json';const lifecycle='./.e2e-runtime-logs/lifecycle.jsonl';let failed;let reportMissing=false;if(fs.existsSync(report)){const s=JSON.parse(fs.readFileSync(report,'utf8')).stats||{};failed=s.unexpected;for(const [k,v] of Object.entries({passed:s.expected,failed,skipped:s.skipped,duration_ms:s.duration}))console.log('| '+k+' | '+(v??'n/a')+' |')}else{reportMissing=true;console.log('| json-report | missing |')}if(!fs.existsSync(lifecycle)){console.log('| worker-lifecycle | missing |');process.exit(0)}const records=fs.readFileSync(lifecycle,'utf8').trim().split('\\n').filter(Boolean).map(JSON.parse);const last=records.at(-1)||{};const lifecycleFailure=/\\.failed$/.test(last.event||'');console.log('| worker-lifecycle | '+(last.event||'unknown')+' |');console.log('| worker-log-path | '+(last.logPath||'n/a')+' |');console.log('| failure-classification | '+(lifecycleFailure?'worker-lifecycle-failure':reportMissing?'report-missing':failed?'test-failure':'passed')+' |')"
            echo
          } >> "$GITHUB_STEP_SUMMARY"

      - name: Upload Playwright report
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: playwright-report-main-${{ matrix.shard }}-of-3
          path: |
            playwright-report/
            playwright-main-${{ matrix.shard }}.log
            .e2e-runtime-logs/
          if-no-files-found: ignore
          retention-days: 7

      - name: Upload test results on failure
        if: failure()
        uses: actions/upload-artifact@v4
        with:
          name: test-results-main-${{ matrix.shard }}-of-3
          path: test-results/
          if-no-files-found: ignore
          retention-days: 7

  e2e-image:
    name: E2E image (${{ matrix.project }})
    runs-on: ubuntu-latest
    timeout-minutes: 30
    if: github.event_name != 'push'
    strategy:
      fail-fast: false
      matrix:
        include:
          - project: image-chromium
            browser: chromium
          - project: image-webkit
            browser: webkit
          - project: image-firefox
            browser: firefox
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup pnpm environment
        uses: ./.github/actions/setup-pnpm

      - name: Resolve Playwright version
        id: playwright-version
        shell: bash
        run: |
          version=$(node -p "require('./node_modules/@playwright/test/package.json').version")
          echo "version=$version" >> "$GITHUB_OUTPUT"

      - name: Record cache restore start
        id: cache-restore-start
        shell: bash
        run: echo "ts=$(date +%s)" >> "$GITHUB_OUTPUT"

      - name: Restore main browser cache
        id: cache-main
        uses: actions/cache/restore@v4
        with:
          path: ~/.cache/ms-playwright
          key: ms-playwright-main-${{ runner.os }}-${{ steps.playwright-version.outputs.version }}-${{ matrix.browser }}

      - name: Restore PR browser cache
        id: cache-pr
        if: steps.cache-main.outputs.cache-hit != 'true' && github.event_name == 'pull_request'
        uses: actions/cache/restore@v4
        with:
          path: ~/.cache/ms-playwright
          key: ms-playwright-pr-${{ runner.os }}-${{ steps.playwright-version.outputs.version }}-${{ matrix.browser }}

      - name: Record cache restore end
        id: cache-restore-end
        shell: bash
        run: echo "ts=$(date +%s)" >> "$GITHUB_OUTPUT"

      - name: Record browser prep start
        id: browser-prep-start
        shell: bash
        run: echo "ts=$(date +%s)" >> "$GITHUB_OUTPUT"

      - name: Install system dependencies
        shell: bash
        run: pnpm exec playwright install-deps ${{ matrix.browser }}

      - name: Install Playwright browser
        if: steps.cache-main.outputs.cache-hit != 'true' && steps.cache-pr.outputs.cache-hit != 'true'
        shell: bash
        run: pnpm exec playwright install ${{ matrix.browser }}

      - name: Save PR browser cache
        if: github.event_name == 'pull_request' && steps.cache-main.outputs.cache-hit != 'true' && steps.cache-pr.outputs.cache-hit != 'true' && matrix.browser != 'chromium'
        uses: actions/cache/save@v4
        with:
          path: ~/.cache/ms-playwright
          key: ms-playwright-pr-${{ runner.os }}-${{ steps.playwright-version.outputs.version }}-${{ matrix.browser }}

      - name: Record browser prep end
        id: browser-prep-end
        shell: bash
        run: echo "ts=$(date +%s)" >> "$GITHUB_OUTPUT"

      - name: Generate E2E test env file
        shell: bash
        run: |
          cat > tests/e2e/.dev.vars <<'EOF'
          ADMIN_PASSWORD=test-admin-password
          SESSION_SECRET=test-session-secret
          LIKE_PEPPER=test-like-pepper
          DEV_LIKE_PEPPER=test-like-pepper
          TURNSTILE_SECRET_KEY=test-turnstile-secret
          SKIP_TURNSTILE=true
          SECURE_COOKIE=false
          LOGIN_MAX_ATTEMPTS=100
          LOGIN_WINDOW_MINUTES=5
          EOF

      - name: Run E2E image tests
        shell: bash
        run: |
          set -o pipefail
          pnpm exec playwright test --project=${{ matrix.project }} 2>&1 | tee playwright-image-${{ matrix.project }}.log

      - name: Summarize project run
        if: always()
        shell: bash
        run: |
          {
            echo "## E2E image ${{ matrix.project }}"
            echo
            echo "| metric | value |"
            echo "|---|---|"
            echo "| main-cache-hit | ${{ steps.cache-main.outputs.cache-hit }} |"
            echo "| pr-cache-hit | ${{ steps.cache-pr.outputs.cache-hit || 'n/a' }} |"
            echo "| cache-restore-seconds | $(( ${{ steps.cache-restore-end.outputs.ts }} - ${{ steps.cache-restore-start.outputs.ts }} )) |"
            echo "| browser-prep-seconds | $(( ${{ steps.browser-prep-end.outputs.ts }} - ${{ steps.browser-prep-start.outputs.ts }} )) |"
            echo "| browser-prep-total-seconds | $(( ${{ steps.browser-prep-end.outputs.ts }} - ${{ steps.cache-restore-start.outputs.ts }} )) |"
            echo "| browsers-size | $(du -sh ~/.cache/ms-playwright 2>/dev/null | cut -f1 || echo n/a) |"
            node -e "const fs=require('fs');const report='./playwright-report/results.json';const lifecycle='./.e2e-runtime-logs/lifecycle.jsonl';let failed;let reportMissing=false;if(fs.existsSync(report)){const s=JSON.parse(fs.readFileSync(report,'utf8')).stats||{};failed=s.unexpected;for(const [k,v] of Object.entries({passed:s.expected,failed,skipped:s.skipped,duration_ms:s.duration}))console.log('| '+k+' | '+(v??'n/a')+' |')}else{reportMissing=true;console.log('| json-report | missing |')}if(!fs.existsSync(lifecycle)){console.log('| worker-lifecycle | missing |');process.exit(0)}const records=fs.readFileSync(lifecycle,'utf8').trim().split('\\n').filter(Boolean).map(JSON.parse);const last=records.at(-1)||{};const lifecycleFailure=/\\.failed$/.test(last.event||'');console.log('| worker-lifecycle | '+(last.event||'unknown')+' |');console.log('| worker-log-path | '+(last.logPath||'n/a')+' |');console.log('| failure-classification | '+(lifecycleFailure?'worker-lifecycle-failure':reportMissing?'report-missing':failed?'test-failure':'passed')+' |')"
            echo
          } >> "$GITHUB_STEP_SUMMARY"

      - name: Upload Playwright report
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: playwright-report-image-${{ matrix.project }}
          path: |
            playwright-report/
            playwright-image-${{ matrix.project }}.log
            .e2e-runtime-logs/
          if-no-files-found: ignore
          retention-days: 7

      - name: Upload test results on failure
        if: failure()
        uses: actions/upload-artifact@v4
        with:
          name: test-results-image-${{ matrix.project }}
          path: test-results/
          if-no-files-found: ignore
          retention-days: 7
