# 测试策略

## 目的

测试必须证明 bootstrap PRD 中的跨层契约：公开/私有数据隔离、四状态审核、软删除、匿名点赞、确定性轮换、PNG 限制、管理员会话，以及 Cloudflare 依赖失败时的行为。

绿地项目没有需要保留的历史行为。首次实现必须建立这些测试，作为后续 Spec 引用的参考示例。

## 测试分层

| 层级 | 工具 | 负责内容 |
|---|---|---|
| 纯单元测试 | Vitest | 领域 helper、schema、轮换、归一化和图片决策 |
| Vue 组件测试 | Vitest + Vue Test Utils | 渲染、交互和无障碍状态 |
| Worker 集成测试 | Cloudflare Workers Vitest integration | Worker fetch、Hono、D1、R2、中间件和 migration |
| 浏览器 E2E | Playwright | 访客和管理员的关键路径 |
| 构建/部署 smoke test | Vite + Wrangler | 生产 bundle、Worker/Assets 路由和 binding 结构 |

不要 mock 被测单元。只 mock 当前测试层不负责的外部边界。

## 契约测试

共享 Zod schema 是可执行的 API 契约。每条路由都必须：

- 解析一份有代表性的成功响应；
- 解析每一种已记录的错误信封；
- 拒绝泄漏任何禁止公开私有字段的响应；
- 测试性质文本的空白/长度约束，以及状态、加群方式和资源用途的枚举穷尽性；
- 验证前端测试夹具由共享 schema 生成，而不是来自单独声明的 interface。

公开投影测试必须明确证明响应中不存在联系方式、审核备注、软删除元数据、内部版本、R2 key、设备 ID 和投票者 hash。

## 单元测试清单

- 搜索归一化：空白、Unicode 兼容性、拉丁字母大小写、中文、空输入和控制字符输入
- 轮换时间窗：`Asia/Shanghai` 时区下 `04:01` 和 `16:01` 之前/当时/之后、日期边界、自定义配置时间、同一时间窗内顺序稳定，以及相邻时间窗按一个位置循环移动
- 配置校验：缺少字段、平台 ID 重复、不支持的加群方式、无效时间字符串和功能开关
- 点赞持久化解析：存储缺失、JSON 格式错误、schema 版本变化和重复 ID
- 图片处理决策：5 MB 以内的 JPG/PNG 原图（不兼容 WebP）、带 alpha 的 PNG、Logo/二维码目标策略、头像一次 PNG 编码、二维码 `0.90 → 0.80 → 0.70` JPEG 质量阶梯、预览元数据、恰好达到/超过最终 128 KB/1 MB 限制、对象 URL 清理，以及压缩后二维码由真实解码器验收
- 错误映射和脱敏 helper

使用注入的虚假时钟。测试不得依赖开发者机器的时区或当前日期。

## Vue 组件测试

必须建立的参考用例：

- 公开 `GroupCard`：平台和性质徽标、Logo 兜底、图片懒加载属性；不得渲染已下架标记或任何已下架群组。管理端状态标记另由管理员组件测试覆盖
- 复制/点赞控件：键盘操作、乐观更新成功、失败回滚、实时反馈
- 提交对话框：必填字段、1–5 个标签、不安全 URL、无图/带图提交、图片处理失败和限流错误
- 管理员认证：会话过期和通用登录失败
- 管理员群聊表单：四种状态、版本冲突、软删除、恢复和永久删除确认
- 图片工具：透明 PNG 预览、二维码白底预览、单次编码，以及超过 128 KB/1 MB 时禁用上传
- 仪表盘：一个依赖不可用时，其他组件仍可使用

优先按无障碍 role 查询，不使用依赖实现细节的 selector。

## Worker 集成测试

使用 Cloudflare Vitest integration 在 `workerd` 中运行生产 `worker/index.ts` 的 API 测试。每个测试文件都在隔离的本地 D1 中应用真实 SQL migration。配置隔离的 R2 binding；只有仍存在的外部 Analytics 边界允许 stub。

质量门禁必须使用 `@cloudflare/vitest-pool-workers` 的隔离存储，不得通过
`getPlatformProxy()` 复用工作区 `.wrangler/state`。setup 应使用
`applyD1Migrations()` 应用当前全部 migration；另设未迁移的 D1 binding，覆盖代表性的
旧版本到最新版本升级。测试运行前后的数据库状态不能依赖开发者机器上一次执行留下的
数据。

必须覆盖：

- 公开列表、搜索、板块成员和详情绝不返回待审核、已拒绝、已下架或软删除群聊
- 游标、query、固定纪元和轮换时间窗一致性
- 纯文本和 multipart 提交校验、图片校验与限流
- `X-Device-Id` 校验、幂等点赞/取消点赞和投票者唯一约束
- 缓存点赞数始终等于点赞行聚合值
- 登录成功/失败/限流、Cookie 篡改、过期、退出登录、Origin，以及会话绑定 `X-CSRF-Token` 的缺失、错误和成功场景
- 管理员 CRUD、所有状态、过期 `version` 和私有投影
- multipart 请求体与最终文件字节限制、用途 MIME、PNG 签名/chunk/IHDR 或 JPEG marker/SOF 尺寸、宽高/总像素、格式校验和生成的 R2 key
- 公开带图片投稿的 ready asset 聚合、D1 失败后的 R2 补偿删除，以及补偿删除失败后的 `delete_failed` 清理记录
- 资源替换顺序和孤立对象安全
- 软删除、恢复、`pending`/`r2_done` 永久清理进度，以及 R2 或 D1 失败后的重试
- D1、R2 和 Analytics 不可用时映射为已记录的错误/部分可用组件

资源与聚合回归测试还必须遵守：

- 使用最小合法二进制 fixture 走真实上传路由，不用固定不存在 UUID 代替 staged/ready
  状态；
- 同时断言 HTTP、D1 行/关联/引用计数和 R2 对象，不以“请求返回 2xx”代替最终状态；
- 用可注入 adapter 制造 R2 delete/head 失败，并证明重试成功数只统计实际删除；
- 并发写入固定同一应用时钟并携带同一旧 version，断言一个成功、一个冲突以及完整聚合
  一致；
- 用数据库 trigger 或等价故障注入让 batch 中途失败，证明主表、关联、资源引用和
  mutation token 全部回滚。
- 对跨聚合共享 staged asset 做并发 adoption，断言数据库实际引用数与 `ref_count`
  完全一致；让 asset 在预校验后变为不可引用时，QR INSERT 必须使聚合 batch 回滚。
- 分页排序测试必须跨越特殊分区（例如“有标签→无标签”）并覆盖尾页，不能只断言单页
  内部有序；LIKE 搜索必须用 `%`、`_` 等元字符证明其按字面子串匹配。
- Vue 组件测试应激活真实用户控制（包括 form 外通过 `form` 属性关联的 submit 按钮）
  并断言只触发一次命令；未保存导航测试必须验证“继续编辑/放弃”两种 Promise 决策。

Workers 测试必须消费 fetch 和 R2 响应体。生产 compatibility flag 必须显式配置，因为测试池可能启用生产环境没有的 Node compatibility。

## Playwright 关键路径

1. 浏览、翻页、搜索、清除搜索、复制群号。
2. 点赞、刷新、确认本地状态、取消点赞。
3. 提交一个有效的纯文本群聊，并看到安全的受理回执。
4. 登录、复核待审核记录、将其发布，并在公开页面看到它。
5. 下架群聊，并确认它仍公开展示且带有标记。
6. 软删除，确认公开页面/管理员列表移除，恢复，然后在确认后永久删除。
7. 将带透明度的 PNG 转为保留 alpha 的 PNG，在 Logo 限制内上传，并拒绝超限结果；压缩二维码后用真实扫码器确认仍可识别且为白底 JPEG，超限按三次质量阶梯处理。
8. 切换当前实现阶段要求的视口和颜色偏好。

关键 E2E 测试至少在一个桌面视口和一个移动视口运行。阶段专属的响应式、深色模式和二维码用例，在相应功能开关启用后成为必测项。

Playwright 的 API 服务必须使用测试专用 Wrangler 配置、本地持久化目录和本地 D1/R2；
启动脚本需在工作区内重建该目录。`No tests found`、浏览器未安装或服务启动失败都属于
门禁失败，不能以组件测试替代。不得接管开发者个人浏览器会话。

## 规模与性能夹具

维护一个确定性的测试夹具，包含：

- 1,000 个混合状态群聊，其中已发布记录用于公开规模基线，已下架记录用于证明公开过滤；
- 多种平台、性质、标签和加群方式组合；
- Logo 失败/缺失用例；
- 不影响顺序的不同点赞数。

断言：

- 初始请求受游标页大小限制；
- 不会提前请求屏幕外 Logo；
- 搜索和轮换结果保持正确；
- 不出现 N+1 API 或 D1 查询模式；
- 已知图片尺寸引起的布局偏移保持在限度内。

首次实现并完成测量后再记录性能预算，不要凭空制定没有依据的数字。相对该基线的性能回退必须有书面决策。

## 必须运行的命令

```bash
pnpm lint
pnpm format:check
pnpm typecheck
pnpm test
pnpm test:workers
pnpm test:e2e
pnpm build
```

除非测试被明确记录为隔离状态，CI 必须运行全部命令。隔离测试必须有负责人、原因和解除条件；反复重试不稳定测试直到通过不属于质量门禁。

## 环境安全

- 测试只使用本地 D1/R2。
- 预览 smoke test 只使用预览 binding。
- 自动化测试不得选择使用生产数据或生产写入 token。
- 测试夹具不得包含真实联系方式、Secret、Cookie、设备 ID 或 Cloudflare API token。
